__pycache__/
*.py[cod]
.venv/
.DS_Store

# Raw annotations and intermediate artifacts — never ship
data/raw/
data/tracklets_raw.parquet

# Held-out eval — lives in the grader, not the starter
data/eval.parquet

# Hashing salt — used by data/build_windows.py; never ship
.hash_salt
catboost_info/

# Generated training artifacts
data/train_full.parquet
data/train_all.parquet
data/train_original.parquet
data/dev_original.parquet
traj_xgb_backup.pkl
traj_xgb_60feat.pkl
traj_xgb_dev.pkl
model_dev.pkl
best_model_ft_*.pt
